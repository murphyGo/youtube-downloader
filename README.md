# youtube-downloader

A YouTube video downloader with a GH Pages web UI and a CLI for agent automation, backed by a small Fly.io proxy.

## Status

In development. See `PLAN.md` for progress.

## Run

TBD — see `PLAN.md` for the up-to-date command list once setup tasks are checked off.
